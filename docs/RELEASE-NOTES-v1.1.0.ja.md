[简体中文](RELEASE-NOTES-v1.1.0.md) | 日本語 | [English](RELEASE-NOTES-v1.1.0.en.md)

# Eizo v1.1.0

## ネイティブ PiP、再生体験と Bangumi コレクション同期

Eizo 1.1 では Windows App SDK の CompactOverlay を利用したネイティブ PiP を追加し、再生エンジンを作り直さずに現在のセッションを維持します。あわせて Bangumi の作品ページで5種類のコレクション状態を読み書きできるようになりました。

## 変更内容

- プレイヤーヘッダー右側に PiP ボタンを追加し、WinUI の標準 Button と Segoe Fluent の MiniExpand アイコンを使用。
- PiP 移行時も現在の PlaybackView をそのまま維持し、LibVLC エンジンをアンロード / 再生成しないため、メディアを開き直しません。
- PiP と全画面は相互排他。小窓ではアプリのタイトルバー、ナビゲーション、プレイヤーサイドバーを隠し、高頻度の再生操作だけを残します。
- 10 秒戻る、再生 / 一時停止、10 秒進む、通常ウィンドウへ戻る操作を提供し、WinUI 標準のホバー / 押下 / フォーカス状態をそのまま利用。
- PiP では映像をクライアント領域いっぱいに広げ、コントロールを映像上の透明グラデーション Overlay に変更。再生中は背景ごと自動非表示になり、マウス移動で再表示します。
- Escape で PiP を終了でき、PiP 内のマウスホイールで音量を調整可能。
- 外部字幕の文字サイズを小窓の高さに応じて自動調整。内蔵字幕がない場合は主字幕と第2字幕を衝突しないよう積み重ね、内蔵字幕トラックが有効な場合は第2外部字幕をウィンドウ上端へ固定して下部の LibVLC 字幕と分離します。
- PiP 前のウィンドウ位置、サイズ、最大化状態を保存し、終了後に復元。Windows 側で CompactOverlay が解除された場合も Eizo の UI 状態を同期。
- PiP のツールチップ / アクセシビリティ名を簡体字中国語、日本語、英語で追加。
- Bangumi の作品詳細に「見たい / 見終わった / 視聴中 / 保留 / 断念」の5状態コントロールを追加。ログイン時は現在の状態を読み込み、選択した状態を Bangumi に直接反映します。
- Bangumi の OAuth コレクション読み書き権限を利用し、API の厳格な検証に合わせて `Content-Type: application/json` を明示。書き込み失敗時は HTTP ステータスとサーバーのエラー内容を表示します。
- 製品 / MSIX バージョンを 1.1.0 / 1.1.0.0 に更新し、専用の受け入れ契約と署名済み x64 受け入れパッケージ工程を追加。

## 確認ポイント

- 再生中に PiP へ移行しても映像 / 音声が継続し、メディアが先頭から再読み込みされないこと。
- 小窓が CompactOverlay として前面に表示され、サイズ変更できること。再生中のコントロール自動非表示とマウス再表示が機能すること。
- 外部メイン字幕 / 第2字幕が継続して表示され、小窓では適切に縮小されること。
- Escape または戻るボタンで終了した後、元のウィンドウサイズ / 最大化状態、サイドバー、通常レイアウトが復元されること。
- Bangumi の5種類のコレクション状態を切り替えた後、新しい状態が選択表示され、作品ページを再読み込みしても Bangumi 側と一致すること。

## 対応環境

- Windows 10 2004 / Windows 11
- x64
