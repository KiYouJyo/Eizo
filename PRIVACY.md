# Eizo Privacy / 隐私说明 / プライバシー

## 简体中文

Eizo 是本地优先的 Windows 影音库。当前版本不会要求账户，也不会将本地媒体文件上传到 Eizo 自有服务器。

应用会在本机保存为正常运行所必需的数据，例如设置、窗口状态、媒体库信息与播放状态。只有在用户主动执行联网功能时才会访问网络，例如检查/下载 GitHub Release 更新或打开项目链接。

Eizo 不会在当前版本中主动收集广告标识符或面向 Eizo 自有后台发送使用分析数据。后续接入 WebDAV 或其他远程媒体源时，网络请求将由用户配置并用于访问对应媒体来源。

第三方组件仍受各自许可证和隐私规则约束。

## 日本語

Eizo はローカル優先の Windows メディアライブラリです。現在のバージョンではアカウントを要求せず、ローカルのメディアファイルを Eizo 独自のサーバーへアップロードしません。

設定、ウィンドウ状態、メディアライブラリ情報、再生状態など、通常動作に必要なデータは端末内に保存されます。ネットワーク通信は、GitHub Release の更新確認・ダウンロードやプロジェクトリンクを開くなど、ユーザーが明示的に開始した操作でのみ発生します。

現在のバージョンでは、Eizo 独自のバックエンドへ広告 ID や利用分析データを自動送信しません。将来 WebDAV などのリモートメディアソースを追加した場合、その通信はユーザーが設定したメディアソースへアクセスするために使用されます。

第三者コンポーネントには各コンポーネントのライセンスおよびプライバシー条件が適用されます。

## English

Eizo is a local-first Windows media library. The current version does not require an account and does not upload local media files to an Eizo-operated server.

Data needed for normal operation, such as settings, window state, media-library information, and playback state, is stored locally. Network access occurs only for user-initiated features, such as checking/downloading GitHub Releases or opening project links.

The current version does not automatically send advertising identifiers or usage analytics to an Eizo-operated backend. If remote media sources such as WebDAV are added later, those network requests will be used to access sources configured by the user.

Third-party components remain subject to their own licenses and privacy terms.
